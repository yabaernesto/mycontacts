services:
  postgres:
    image: bitnami/postgresql
    ports:
      - 5432:5432
    environment:
      - POSTGRES_USER=root
      - POSTGRES_PASSWORD=root
      - POSTGRES_DB=pg
    volumes:
      - postgres_data:/bitnami/postgresql

volumes:
  postgres_data:
